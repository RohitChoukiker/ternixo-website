import { ArrowUpRight, LockKeyhole } from 'lucide-react'
import { tools } from '@/data/tools'

const accents = { git: 'var(--accent-git)', docker: 'var(--accent-docker)', linux: 'var(--accent-linux)' }
export function ToolsGrid() {
  return <section id="tools" className="border-b border-border py-24 lg:py-32"><div className="mx-auto max-w-6xl px-5 lg:px-8">
    <div className="mb-12 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="mb-3 font-mono text-[11px] uppercase tracking-[.14em] text-[var(--text-muted)]">01 / choose your surface</p><h2 className="text-3xl font-semibold leading-tight tracking-[-.035em] sm:text-4xl">Tools for the work<br />you want to do.</h2></div><p className="max-w-xs text-sm leading-6 text-[var(--text-secondary)]">Start with the workflow that feels familiar. The fastest way to learn is to make a small thing move.</p></div>
    <div className="grid gap-5 md:grid-cols-3">{tools.map((tool) => { const Icon = tool.icon; const live = tool.status === 'live'; const accent = accents[tool.accent as keyof typeof accents]; const style = { borderLeftColor: accent, '--focus-accent': accent, '--tool-accent': accent } as React.CSSProperties; const content = <><div className="flex items-center justify-between border-b border-border px-4 py-3"><div className="flex gap-2"><span className="size-2 rounded-full bg-[var(--text-muted)]" /><span className="size-2 rounded-full bg-[var(--text-muted)]" /><span className="size-2 rounded-full bg-[var(--text-muted)]" /></div><span className="font-mono text-[9px] text-[var(--text-muted)]">{tool.name.toLowerCase()} — lab</span><Icon style={{ color: accent }} className="size-4" /></div><div className="flex min-h-72 flex-col p-5"><div><span style={{ color: accent, background: `color-mix(in srgb, ${accent} 12%, transparent)` }} className="inline-flex items-center gap-1.5 px-2 py-1 font-mono text-[9px] uppercase tracking-wider">{live ? <span className="size-1.5 rounded-full bg-current" /> : <LockKeyhole className="size-2.5" />}{tool.status}</span><h3 className="mt-7 text-xl font-semibold">{tool.name}</h3><p className="mt-3 text-sm leading-6 text-[var(--text-secondary)]">{tool.description}</p></div><div className="mt-auto border-t border-border pt-5 font-mono text-[11px]"><span style={{ color: accent }}>$ </span><span>{tool.command}</span>{live && <ArrowUpRight style={{ color: accent }} className="ml-2 inline size-3.5 transition-transform group-hover:-translate-y-0.5 group-hover:translate-x-0.5" />}</div></div></>; return live ? <a key={tool.name} href={tool.href} target="_blank" rel="noreferrer" aria-label={`Open ${tool.name} hands-on labs`} style={style} className="group border border-l-2 border-border bg-[var(--bg-elevated)] transition-colors hover:border-[var(--tool-accent)]">{content}</a> : <div key={tool.name} aria-disabled="true" style={style} className="border border-l-2 border-border bg-[var(--bg-elevated)] opacity-80 transition-colors hover:border-[var(--tool-accent)]">{content}</div> })}</div>
  </div></section>
}
