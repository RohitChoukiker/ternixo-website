import { ArrowUpRight, Clock3 } from 'lucide-react'
import { tools } from '@/data/tools'

export function ToolsGrid() {
  return <section id="tools" className="border-b border-border py-24 lg:py-32"><div className="mx-auto max-w-6xl px-5 lg:px-8"><div className="mb-12 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="mb-3 font-mono text-xs uppercase tracking-[0.18em] text-primary">01 / choose your surface</p><h2 className="text-3xl font-semibold tracking-[-0.04em] text-foreground sm:text-4xl">Tools for the work<br />you want to do.</h2></div><p className="max-w-xs text-sm leading-6 text-muted-foreground">Start with the workflow that feels familiar. The fastest way to learn is to make a small thing move.</p></div><div className="grid gap-4 md:grid-cols-3">{tools.map((tool) => { const Icon = tool.icon; const live = tool.status === 'live'; return <a key={tool.name} href={tool.href} target={live ? '_blank' : undefined} rel={live ? 'noreferrer' : undefined} className={`group flex min-h-80 flex-col rounded-lg border border-border bg-card p-6 transition-all ${live ? 'hover:-translate-y-1 hover:border-primary/60 hover:shadow-[0_16px_40px_-24px_var(--primary)]' : 'opacity-75'}`}><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-md border border-border bg-background text-primary"><Icon className="size-5" /></div>{live ? <span className="inline-flex items-center gap-1.5 rounded-full border border-primary/25 bg-primary/10 px-2.5 py-1 font-mono text-[10px] text-primary"><span className="size-1.5 rounded-full bg-primary" /> live</span> : <span className="inline-flex items-center gap-1.5 rounded-full border border-border px-2.5 py-1 font-mono text-[10px] text-muted-foreground"><Clock3 className="size-3" /> soon</span>}</div><div className="mt-auto"><h3 className="mt-12 text-xl font-semibold tracking-tight text-foreground">{tool.name}</h3><p className="mt-3 text-sm leading-6 text-muted-foreground">{tool.description}</p><div className="mt-6 flex items-center justify-between border-t border-border pt-4"><code className="max-w-[82%] truncate font-mono text-[11px] text-muted-foreground">$ {tool.command}</code>{live && <ArrowUpRight className="size-4 shrink-0 text-primary transition-transform group-hover:translate-x-0.5 group-hover:-translate-y-0.5" />}</div></div></a> })}</div></div></section>
}
